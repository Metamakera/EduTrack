const express = require('express');
const { loginTeacher } = require('../controllers/teacherController'); // Import controller function
const router = express.Router();

// POST route for teacher login


module.exports = router;
